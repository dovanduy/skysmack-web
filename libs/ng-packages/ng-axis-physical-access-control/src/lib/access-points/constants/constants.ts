// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const ACCESS_POINTS_REDUCER_KEY = 'accessPoints';
export const ACCESS_POINTS_AREA_KEY = 'access_points'; // validation, store, menu
export const ACCESS_POINTS_REDUX_KEY = 'ACCESS_POINTS_'; // reducer prefix, action, epics, Requests
export const ACCESS_POINTS_ADDITIONAL_PATHS = ['access-points']; // actions, requests
