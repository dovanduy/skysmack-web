// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const AXIS_PHYSICAL_ACCESS_CONTROL_AREA_KEY = 'axis_physical_access_control'; // validation, store, menu
export const AXIS_PHYSICAL_ACCESS_CONTROL_ADDITIONAL_PATHS = []; // actions, requests
