// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const CORS_REDUCER_KEY = 'cors';
export const CORS_AREA_KEY = 'cors'; // validation, store, menu
export const CORS_REDUX_KEY = 'CORS_'; // reducer prefix, action, epics, Requests
export const CORS_ADDITIONAL_PATHS = []; // actions, requests