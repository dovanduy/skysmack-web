// Ensure area key is the same as the reducer name without the "Reducer" part.
export const EMAILS_SMTP_REDUCER_KEY = 'emailsSmtp';
export const EMAILS_SMTP_AREA_KEY = 'emails_smtp'; // validation, store, menu
export const EMAILS_SMTP_REDUX_KEY = 'EMAILS_SMTP'; // reducer prefix, action, epics, Requests
export const EMAILS_SMTP_ADDITIONAL_PATHS = []; // actions, requests

