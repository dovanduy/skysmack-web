// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const FILE_STORAGE_REDUCER_KEY = 'fileStorage';
export const FILE_STORAGE_AREA_KEY = 'file-storage'; // validation, store, menu
export const FILE_STORAGE_REDUX_KEY = 'FILE_STORAGE_'; // reducer prefix, action, epics, Requests
export const FILE_STORAGE_ADDITIONAL_PATHS = []; // actions, requests