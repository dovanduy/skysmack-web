import { FIND, ADD, UPDATE, REMOVE, FIELDS } from '@skysmack/framework';

export class FileStoragePermissions {
    private static fileStorage = 'FileStorage';
    private static personFields = 'FileStorage' + FIELDS;
}