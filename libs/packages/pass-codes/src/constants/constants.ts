// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const PASS_CODES_REDUCER_KEY = 'passCodes';
export const PASS_CODES_AREA_KEY = 'pass-codes'; // validation, store, menu
export const PASS_CODES_REDUX_KEY = 'PASS_CODES_'; // reducer prefix, action, epics, Requests
export const PASS_CODES_ADDITIONAL_PATHS = []; // actions, requests