// Ensure area key is the same as the reducer name without the "Reducer" part.
export const PERSONS_REDUCER_KEY = 'persons';
export const PERSONS_AREA_KEY = 'persons'; // validation, store, menu
export const PERSONS_REDUX_KEY = 'PERSONS_'; // reducer prefix, action, epics, Requests
export const PERSONS_ADDITIONAL_PATHS = []; // actions, requests