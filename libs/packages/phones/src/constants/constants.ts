// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const PHONES_REDUCER_KEY = 'persons';
export const PHONES_AREA_KEY = 'persons'; // validation, store, menu
export const PHONES_REDUX_KEY = 'PHONES_'; // reducer prefix, action, epics, Requests
export const PHONES_ADDITIONAL_PATHS = []; // actions, requests