// Ensure area key is the same as the reducer name without the "Reducer" part.
export const PACKAGES_AREA_KEY = 'packages'; // validation, registerRedux, store, menu
export const PACKAGES_REDUX_KEY = 'PACKAGES_'; // reducer prefix, action, epics, Requests
export const PACKAGES_ADDITIONAL_PATHS = []; // actions, requests
