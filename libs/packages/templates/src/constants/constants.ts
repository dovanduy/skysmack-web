// Ensure reducer key is the same as the reducer name without the "Reducer" part.
export const TEMPLATES_REDUCER_KEY = 'templates';
export const TEMPLATES_AREA_KEY = 'templates'; // validation, store, menu
export const TEMPLATES_REDUX_KEY = 'TEMPLATES_'; // reducer prefix, action, epics, Requests
export const TEMPLATES_ADDITIONAL_PATHS = []; // actions, requests