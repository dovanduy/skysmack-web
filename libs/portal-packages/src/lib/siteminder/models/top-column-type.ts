export enum TopColumnType {
    /**
     * Shows rate plans at the top (one for each).
     * Channels are shown as expanded data.
     */
    RatePlans,
    /**
    * Shows channels at the top (one for each).
    * Rates are shown as expanded data.
    */
    Channels
}