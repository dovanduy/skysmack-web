export enum FieldTypes {
    // Returned from backend
    int,
    string,
    dateTime,
    decimal,
    double,
    geography,
    // Only defined in front end
    CheckboxField,
    DateField,
    EmailField,
    HiddenField,
    limitedString,
    PasswordField,
    RecurringExpressionField,
    SelectField,
    TimeField,
}
