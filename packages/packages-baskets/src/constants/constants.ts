// Ensure area key is the same as the reducer name without the "Reducer" part.
export const BASKETS_AREA_KEY = 'baskets'; // validation, registerRedux, store, menu
export const BASKETS_REDUX_KEY = 'BASKETS_'; // reducer prefix, action, epics, Requests
export const BASKETS_ADDITIONAL_PATHS = []; // actions, requests