// Ensure area key is the same as the reducer name without the "Reducer" part.
export const IDENTITES_AREA_KEY = 'identities'; // validation, registerRedux, store, menu
export const IDENTITES_REDUX_KEY = 'IDENTITES_'; // reducer prefix, action, epics, Requests
export const IDENTITES_ADDITIONAL_PATHS = []; // actions, requests

export const ROLES_AREA_KEY = 'roles'; // validation, registerRedux, store, menu
export const ROLES_REDUX_KEY = 'ROLES_'; // reducer prefix, action, epics, Requests
export const ROLES_ADDITIONAL_PATHS = []; // actions, requests

export const USERS_AREA_KEY = 'users'; // validation, registerRedux, store, menu
export const USERS_REDUX_KEY = 'USERS_'; // reducer prefix, action, epics, Requests
export const USERS_ADDITIONAL_PATHS = []; // actions, requests