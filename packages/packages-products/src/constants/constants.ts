// Ensure area key is the same as the reducer name without the "Reducer" part.
export const PRODUCTS_AREA_KEY = 'products'; // validation, registerRedux, store, menu
export const PRODUCTS_REDUX_KEY = 'PRODUCTS_'; // reducer prefix, action, epics, Requests
export const PRODUCTS_ADDITIONAL_PATHS = []; // actions, requests